# 剧情版 · 本地实现记录

状态：2026-09-18 随 GalGame 开发分支提交。以下各轮的“未上传”表示当时的检查状态，本次一并上传；SDK 仍为本地可选试验。

已实现：新首页标题、每次打开和回主菜单的精灵入场；同一原任务的新存档开场请求；结构化召唤、传送、点赞、慢眨眼、草地终章和道具交付；两帧草地鼓掌；真实文档阅读入口；三人人设和适量闲聊导演规则。

内容约束：剧情玩家台词独立标记，不能成为真实用户回答或授权；全部剧情正式文本按原同步机制发布。草案和未闭合方案不允许触发通关结局。旧存档不补写剧情。

已检查：TypeScript 构建、18 项针对性剧情/回看测试；浏览器离线演示走过开场→传送→点赞→眨眼→小黑相遇→草地→文档，道具可打开真实示例 Markdown；结局帧已加载（后续调整为张手、合掌两帧切换）。演示数据无模型，不冒充原宿主的真人试用。

未在本轮重测：Codex / WorkBuddy 新完整审查会话、不同大模型长期人设稳定性、macOS。新桥段由原 Agent 遵照 Skill 输出；页面不会改写普通回复来伪装角色台词或通过审查。

生图：使用内置 imagegen。生成的点赞立绘、草地画面、文档道具及完整提示词记录在 `web/assets/midnight/story-art-prompts.json`。没有增加模型服务、浏览器密钥或额外付费依赖。

后续修正：撤回人物多帧循环，乔布斯/小黑恢复原静帧；系统精灵恢复默认 Live2D（保留用户关闭选项和失败回退），结局仅张手、合掌两帧。
# 2026-09-18：精灵召唤阵出场

- 使用内置生图生成透明蓝金召唤阵：`web/assets/midnight/spirit-summoning-circle.png`；完整提示词记录在相邻 `spirit-summoning-prompt.json`。
- 主菜单与剧情 summon 共用 2.8 秒编排：地面光阵亮起、轻微旋转，420ms 后精灵淡入并上浮32px，第二道淡光环上升消散，结束接回原 Live2D。原先的大幅缩放、模糊、增亮均移除。
- 等待贴图解码及 Live2D 初始化，避免在出场中途突然切换渲染器；初始化最多等2.5秒，失败继续静帧。切场、回看、重复触发、后台隐藏会清理动画，减少动态效果时直接显示。
- Live2D 画布尺寸改用未变换的布局尺寸，避免按 CSS 缩放后的包围盒逐帧调整 WebGL 缓冲区。
- 本地构建、7项相关测试通过；浏览器确认透明召唤阵和 Live2D 加载。未做跨设备帧率基准测试；未上传。

## 2026-09-18：章节进度与 Jobs 工作室精修

- 新生成三主题轨道图集与银白磨砂面板，素材及完整提示词保存在 `web/assets/midnight/studio-progress-prompts.json`。使用内置 imagegen，透明 PNG 已检查。
- 左侧进度轨道分别采用蓝金星轨、银色刻度、黑红侦探风格；显示共鸣/共识/信任及实际百分比。悬停、键盘聚焦或点击聚焦展开已确认节点。没有编造进度、增加必答问题或修改存档数据。
- Jobs 统一字体层级、银白面板、紧凑角色署名、悬浮图标工具条、悬停标签、蓝色确认按钮、淡蓝选中态、聚焦输入框，以及存档/设置/阅读器等弹窗。较矮桌面窗口使用紧凑回答布局，避免确认按钮被挤出可见区域。
- 构建、6项播放/回看测试通过。离线浏览器实际查看了 Jobs 进度35%、两条节点清单、选项选中态与确认按钮。未向原宿主发送测试消息，未发布。

## 本地后续：进度内部填充

- 轨道加宽，内部填充从2px扩展为14px（小黑22px），底部按真实百分比上升。系统暖金、Jobs蓝色、小黑银白；保留深色空槽、清晰液面和独立节点，0%为空、100%填满。
- 小黑不再使用红蓝装饰轨道，直接复用原黑白切角按钮 `xiaohei-panel.png`，节点改为黑白横刻度。没有新生成素材或更改进度计算。
- 离线预览可设 `GALGAME_FIXTURE_PROGRESS=1`，独立测试三主题0/35/100%。类型检查和6项播放测试通过，浏览器实际检查金色部分填充、Jobs满格、小黑银白部分填充。本次尚未上传。

## 本地后续：终章晨光星轨 UI

- 内置 imagegen 生成 `meadow-panel.png` 和 `dialogue-icons-meadow.png`，沿用系统精灵的星轨纹饰，改为象牙白、浅天蓝与香槟金；提示词见 `web/assets/midnight/finale-ui-prompts.json`。
- 终章标题栏、对话框、角色名牌、按钮、图标、进度轨道与弹窗统一换肤；文档道具从105px放大到210px，交付面板加宽至780px，小窗口使用160px道具。
- 构建与10项服务端检查通过，离线浏览器确认终章布局、按钮可见及点击道具打开文档；本地正式服务已重新加载。未向原 Agent 发送测试消息，未上传 GitHub。

### 终章边缘修正

- 终章/交付阶段隐藏进度条及节点提示，回看前面的问答章节仍可显示对应进度。
- 生成连续圆角金边 `meadow-panel-clean.png`，替换缩小后产生碎线的复杂纹饰；按钮和名牌使用完整素材缩放，图标控件取消误覆盖的面板边框。提示词保存在 `finale-clean-prompt.json`。
- 构建和前端语法检查通过，离线浏览器确认终章无进度条、按钮边缘连续、图标无多余方框。未上传。
